const reducer = (state = 'NOP', action) => {
	console.log('state now: ', state)
	console.log('action', action)
	// switch(action.type) {
	// case 'SET_MESSAGE':
		
	// }
	return state
}

export default reducer
